# D-06 최종 설정 (plan v2.12). test에서 평가할 실험: 구역 -> 거리(1/3/6 = 10/30/60분) -> 계열.
# 초안: results/horizon.md(val MAE 선택 규칙)로 만든 것. 검토 후 커밋하고,
# "설정 동결, test 실행" 지시가 있을 때만 `tp test --final configs/final.yaml --confirm`을 한 번 실행한다.
zones:
  1:  # 구역 1 (5161)
    1:  # 10분 뒤
      naive_144: EXP-013  # 어제 같은 시각
      naive_1: EXP-014  # 마지막 관측값
      naive_1008: EXP-026  # 지난주 같은 시각
      arima: EXP-017  # ARIMA
      lstm: EXP-016  # LSTM
    3:  # 30분 뒤
      naive_144: EXP-035  # 어제 같은 시각
      naive_1: EXP-029  # 마지막 관측값
      naive_1008: EXP-041  # 지난주 같은 시각
      arima: EXP-047  # ARIMA
      lstm: EXP-053  # LSTM
    6:  # 60분 뒤
      naive_144: EXP-036  # 어제 같은 시각
      naive_1: EXP-030  # 마지막 관측값
      naive_1008: EXP-042  # 지난주 같은 시각
      arima: EXP-048  # ARIMA
      lstm: EXP-054  # LSTM
  2:  # 구역 2 (5059)
    1:  # 10분 뒤
      naive_144: EXP-018  # 어제 같은 시각
      naive_1: EXP-020  # 마지막 관측값
      naive_1008: EXP-027  # 지난주 같은 시각
      arima: EXP-022  # ARIMA
      lstm: EXP-024  # LSTM
    3:  # 30분 뒤
      naive_144: EXP-037  # 어제 같은 시각
      naive_1: EXP-031  # 마지막 관측값
      naive_1008: EXP-043  # 지난주 같은 시각
      arima: EXP-049  # ARIMA
      lstm: EXP-055  # LSTM
    6:  # 60분 뒤
      naive_144: EXP-038  # 어제 같은 시각
      naive_1: EXP-032  # 마지막 관측값
      naive_1008: EXP-044  # 지난주 같은 시각
      arima: EXP-050  # ARIMA
      lstm: EXP-056  # LSTM
  3:  # 구역 3 (5259)
    1:  # 10분 뒤
      naive_144: EXP-019  # 어제 같은 시각
      naive_1: EXP-021  # 마지막 관측값
      naive_1008: EXP-028  # 지난주 같은 시각
      arima: EXP-023  # ARIMA
      lstm: EXP-025  # LSTM
    3:  # 30분 뒤
      naive_144: EXP-039  # 어제 같은 시각
      naive_1: EXP-033  # 마지막 관측값
      naive_1008: EXP-045  # 지난주 같은 시각
      arima: EXP-051  # ARIMA
      lstm: EXP-057  # LSTM
    6:  # 60분 뒤
      naive_144: EXP-040  # 어제 같은 시각
      naive_1: EXP-034  # 마지막 관측값
      naive_1008: EXP-046  # 지난주 같은 시각
      arima: EXP-052  # ARIMA
      lstm: EXP-058  # LSTM
